# Claude Code Proxy 配置文件
#
# 推荐位置：~/.claude/proxy.env
#
# 其他位置（按顺序检查）：
#   1. ./.env（本地项目覆盖）
#   2. ~/.claude/proxy.env（推荐）
#   3. ~/.claude-code-proxy

# ============================================================================
# 提供商配置（OpenAI 兼容端点，支持本地端点）
# ============================================================================

OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-proj-your-openai-key

# OpenAI 兼容端点模型路由示例：
ANTHROPIC_DEFAULT_OPUS_MODEL=google/gemini-3-pro-preview
ANTHROPIC_DEFAULT_SONNET_MODEL=google/gemini-3-flash-preview
ANTHROPIC_DEFAULT_HAIKU_MODEL=google/gemini-2.5-pro

# ============================================================================
# 可选 - 模型路由覆盖
# ============================================================================

# 使用特定模型覆盖基于模式的路由
# 如果未设置，使用智能模式匹配

# Opus 层级（默认：gemini-3-pro-preview）
# ANTHROPIC_DEFAULT_OPUS_MODEL=google/gemini-3-pro-preview

# Sonnet 层级（默认：gemini-3-flash-preview）
# ANTHROPIC_DEFAULT_SONNET_MODEL=google/gemini-3-flash-preview

# Haiku 层级（默认：gemini-2.5-pro）
# ANTHROPIC_DEFAULT_HAIKU_MODEL=google/gemini-2.5-pro

# ============================================================================
# 可选 - 安全
# ============================================================================

# 客户端 API 密钥验证（可选）
# 如果设置，客户端必须提供此确切密钥
# ANTHROPIC_API_KEY=your-validation-key

# ============================================================================
# 可选 - 服务器设置
# ============================================================================

# 服务器主机（默认：0.0.0.0）
# HOST=0.0.0.0

# 服务器端口（默认：8082）
# PORT=8082

# ============================================================================
# 可选 - 高级
# ============================================================================

# 直通模式 - 直接代理到 Anthropic API，不进行转换（默认：false）
# 用于调试或当您想直接使用 Anthropic API 时
# PASSTHROUGH_MODE=false
